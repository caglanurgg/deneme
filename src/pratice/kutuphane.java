package pratice;

public class kutuphane {
/*
    public static void main(String[] args) {
        ArrayList<Kitap> kütüphane = new ArrayList<>();
        Scanner tarayıcı = new Scanner(System.in);

        while (true) {
            System.out.println("1. Kitap Ekle");
            System.out.println("2. Kitapları Listele");
            System.out.println("3. Çıkış");
            System.out.print("Seçiminizi yapın: ");
            int seçim = tarayıcı.nextInt();
            tarayıcı.nextLine(); // Yeni satır karakterini tüket

            if (seçim == 1) {
                System.out.print("Kitap adı: ");
                String başlık = tarayıcı.nextLine();
                System.out.print("Yazar adı: ");
                String yazar = tarayıcı.nextLine();
                kitapEkle(kütüphane, başlık, yazar);
            } else if (seçim == 2) {
                kitaplarıListele(kütüphane);
            } else if (seçim == 3) {
                System.out.println("Programdan çıkılıyor...");
                break;
            } else {
                System.out.println("Geçersiz seçim!");

            }
        }
    }

    public static void kitapEkle(ArrayList<Kitap> kütüphane, String başlık, String yazar) {
        Kitap kitap = new Kitap(başlık, yazar);
        kütüphane.add(kitap);
        System.out.println("Kitap eklendi.");
    }

    public static void kitaplarıListele(ArrayList<Kitap> kütüphane) {
        System.out.println("Kitap Kütüphanesi:");
        for (int i = 0; i < kütüphane.size(); i++) {
            System.out.println((i + 1) + ". " + kütüphane.get(i).getBaşlık() + " - " + kütüphane.get(i).getYazar());
        }
    }
}

class Kitap {
    private String başlık;
    private String yazar;

    public Kitap(String başlık, String yazar) {
        this.başlık = başlık;
        this.yazar = yazar;
    }

    public String getBaşlık() {
        return başlık;
    }

    public String getYazar() {
        return yazar;

 */
    }

